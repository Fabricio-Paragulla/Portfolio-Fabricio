apiVersion: v1
kind: PersistentVolume
metadata:
  name: portfolio-pv-volume
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data/portfolio-logs"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: portfolio-pvc-claim
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi